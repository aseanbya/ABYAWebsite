import { PageSubSection } from "~/components/common/PageSubSection";

export default function ExplorePreviousEvents() {
    return (
        <PageSubSection title="See the previous events">
            <p>To be completed after refactor.</p>
        </PageSubSection>
    );
}
